#include <stdio.h>
// #include "HncZLib.h"

int main() {
  printf("calling HncGZOpenRead(nullptr, nullptr)\n");

  // HncGZOpenRead(nullptr, nullptr);

  printf("called HncGZOpenRead(nullptr, nullptr)\n");
  return 0;
}